// Biomass to CO2 equivalent conversion factor (tCO2 per Mg biomass)
export const BIOMASS_TO_CO2_FACTOR = 3.67;

// Default price per carbon credit in USD
export const DEFAULT_PRICE_PER_CREDIT = 3;

// Pagination
export const DEFAULT_PAGE_SIZE = 5;

// Notification polling intervals (milliseconds)
export const POLL_INTERVAL_MIN_MS = 30_000;
export const POLL_INTERVAL_MAX_MS = 300_000;
export const POLL_FETCH_GUARD_MS = 5_000;
